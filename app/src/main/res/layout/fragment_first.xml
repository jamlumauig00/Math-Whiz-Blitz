<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/bg"
    tools:context=".Math.Fragment.FirstFragment">

    <ImageView
        android:id="@+id/image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/navbar" />

    <TextView
        android:id="@+id/textview_first"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/image"
        android:gravity="center"
        android:textColor="@color/white"
        android:text="Enhance math skills, inspire learning\n through interactive, engaging quiz challenges" />


    <ImageView
        android:id="@+id/imagea"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/textview_first"
        android:layout_above="@id/button_first"
        android:padding="20dp"
        android:src="@drawable/maths" />

    <Button
        android:id="@+id/button_first"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:padding="18sp"
        android:background="@drawable/btn_bg"
        android:layout_marginVertical="10dp"
        android:layout_marginBottom="10dp"
        app:backgroundTint="@null"
        android:textColor="@color/black"
        android:layout_marginHorizontal="30dp"
        android:text="@string/next" />


</RelativeLayout>
